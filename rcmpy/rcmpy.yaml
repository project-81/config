---
includes:
  - includes/linux.yaml
  - includes/vim.yaml

files:
  - template: gitconfig
    name: .gitconfig
    directory: "~"

  - template: userfs.yaml
    name: config.yaml
    directory: "~/.config/userfs"
